import { test } from '../fixtures/pageFixtures';
import { expect } from '@playwright/test';
import { logStep } from '../utils/logger';

test('Place order while registering during checkout', async ({ homePage, cartPage, dashboardPage, accountInfoPage}) => {
  // your steps here
});
